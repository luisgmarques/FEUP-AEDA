#include <vector>
#include "Book.h"

using namespace std;

class Reader {
    private:
    Book books[3];
    const int phoneNumber;
    const string email;

    public:
    Reader(int phoneNumber, string email);

    
};