#include <string>

using namespace std;

/**
 * @brief 
 * 
 */
class Book {
    private:
    string title;
    string isbn;
    string[] authors;
    int pages;

    public:
    Book(string title, string isbn, string[] authors, pages);
    
}